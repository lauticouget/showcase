version: 1
applications:
  - appRoot: 'apps/web'
    frontend:
      phases:
        preBuild:
          commands:
            - corepack enable
            - corepack prepare pnpm@9.7.1 --activate
            - pnpm config set node-linker hoisted
            - pnpm install --frozen-lockfile
        build:
          commands:
            - pnpm nx build web
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - '.next/cache/**/*'